# Ranch Expense Tracker v1.4.6 — GitHub Upload

Replace/upload these four files in the repository root:

- `index.html`
- `service-worker.js`
- `manifest.webmanifest`
- `version.json`

Suggested commit:

`v1.4.6: fix iPhone PDF download handling`

After the green GitHub Pages deployment, test **Download PDF** on the iPhone. It should target one `.pdf` file rather than the previous PDF/share behavior.
